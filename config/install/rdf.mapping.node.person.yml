langcode: en
status: true
dependencies:
  config:
    - node.type.person
  enforced:
    module:
      - islandora_metadata_test
  module:
    - node
id: node.person
targetEntityType: node
bundle: person
types:
  - 'schema:Person'
fieldMappings:
  title:
    properties:
      - 'schema:name'
  field_date_of_birth:
    properties:
      - 'schema:givenName'
  field_date_of_death:
    properties:
      - 'schema:inLanguage'
